dev:
  listen: 0.0.0.0:5003
  verbose: true
  log:
    level: info
  storage:
    driver: <storage-driver>
    ...
  task_storage:
    driver: <task-storage-driver>
    ...
  service_endpoint:
    default:
      address: <metathings-address>
  credential:
    domain: <credential-domain>
    id: <credential-id>
    secret: <credential-key>
